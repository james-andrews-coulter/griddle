services:
  rss-griddle:
    build: .
    ports:
      - "4080:4080"
    volumes:
      - ./data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:4080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
